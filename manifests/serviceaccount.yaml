apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Name }}
  namespace: {{ .Namespace }}
  labels:
  {{- if .Labels }}
    {{- .Labels | yaml | nindent 4 }}
  {{- end }}
  annotations: {}
{{- if .Secrets }}
secrets:
{{- range .Secrets }}
- name: {{ . }}
{{- end }}
{{- end }}
